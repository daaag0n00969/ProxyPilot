# Security

ProxyPilot installs and loads a kernel packet-filter driver (WinDivert). Run only binaries you built or trust. Always start it as Administrator; that is required by the driver, not optional.

Report vulnerabilities privately: open a GitHub Security advisory on this repository, or contact the owner via GitHub.

Do not file public issues that include exploit PoCs against the intercept path.
